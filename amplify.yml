version: 1
frontend:
   phases:
       preBuild:
           commands:
               - npm install
       build:
           commands:
               - env | grep -e DYNAMO_DB_URL -e S3_BUCKET_URL -e REQUEST_URL -e GALLERY_BUCKET -e GALLERY_ACCESS_KEY -e GALLERY_ACCESS_KEY_ID >> .env.production
               - npm run build
   artifacts:
       baseDirectory: .next
       files:
           - '**/*'
   cache:
       paths:
           - 'node_modules/**/*'
